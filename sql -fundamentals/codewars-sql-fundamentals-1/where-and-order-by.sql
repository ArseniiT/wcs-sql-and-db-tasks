-- create a SELECT statement that will return all columns from the people table WHERE their age is over 50
-- order by the age descending


SELECT
    *
FROM
    people
WHERE
    age > 50
ORDER BY
    age DESC;
