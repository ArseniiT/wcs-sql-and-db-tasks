-- simple SELECT statement that will return all columns from the people table WHERE their age is over 50
-- and order by the age descending


SELECT
    *
FROM
    people
WHERE
    age > 50
ORDER BY
    age DESC;
